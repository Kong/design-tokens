/**
 * The static token prefix.
 * !IMPORTANT: The same prefix **must be used** in both `@kong/design-tokens` and `@kong/portal-design-tokens` source files.
 */
export const KONG_TOKEN_PREFIX = 'kui'
/** The tokens directory, inside the build directory to source the built tokens from */
export const TOKEN_DIRECTORY = 'dist/tokens'
